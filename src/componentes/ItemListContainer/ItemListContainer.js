const ItemListContainer =({greeting}) => {
    return (
        <div className="mt-20">
            <h2 className="text-center text 2x1 font-bold">{greeting}</h2>
        </div>
    );
}

export default ItemListContainer